/// <reference types="react" />
export interface Cursor {
    results: any[];
    nextPage: () => Promise<Cursor>;
    previousPage: () => Promise<Cursor>;
    totalPages: number;
    page: number;
}
interface Params {
    typeName: string;
    pageSize: number;
    selectedColumns: Set<string>;
}
declare function usePaginatedClient({ typeName, pageSize, selectedColumns }: Params): {
    results: any[];
    page: number;
    totalPages: number;
    setPage: import("react").Dispatch<import("react").SetStateAction<number>>;
    loading: boolean;
    pageIds: string[];
    total: number;
    refresh: () => void;
};
export default usePaginatedClient;
