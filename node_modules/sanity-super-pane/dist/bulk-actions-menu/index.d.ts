/// <reference types="react" />
interface Props {
    className?: string;
    typeName: string;
    selectedIds: Set<string>;
}
declare function BulkActionsMenu({ className, selectedIds, typeName }: Props): JSX.Element;
export default BulkActionsMenu;
